<?xml version="1.0" encoding="UTF-8"?>
<wodefinitions>
	<wo class="AjaxBusySpinner" wrapsContent="false">

		<doc><![CDATA[Shows a busy indicator while AjaxSlim has one or more Ajax requests in flight, and hides it when idle. A pure-CSS spinner driven by the runtime's activity broker — `ajaxslim.js` sets `data-ajaxslim-busy` on the document element while any request is in flight. No configuration: a busy indicator is the same everywhere. (Apps wanting a bespoke one can style `.ajaxslim-busy-spinner` or react to the `ajaxslim:busy`/`ajaxslim:idle` events on `document`.)

```html
<wo:AjaxBusySpinner/>
```]]></doc>

		<binding name="id">
			<pull><type>java.lang.String</type></pull>
			<doc>Optional id for the spinner element.</doc>
		</binding>

		<binding name="class">
			<pull><type>java.lang.String</type></pull>
			<doc>Optional extra CSS class(es) on the spinner.</doc>
		</binding>

		<binding name="style">
			<pull><type>java.lang.String</type></pull>
			<doc>Optional inline style on the spinner.</doc>
		</binding>

		<binding name="delay">
			<pull><type>java.lang.String</type></pull>
			<doc><![CDATA[Optional show-delay (anti-flash): the spinner only appears after this much sustained Ajax activity. A bare number is seconds (`delay="0.2"`); a CSS time value with its own unit (`"200ms"`) is also accepted.]]></doc>
		</binding>

		<binding name="fade">
			<pull><type>java.lang.String</type></pull>
			<doc><![CDATA[Optional fade-in/out duration: the spinner fades over this time instead of appearing and vanishing instantly. Same value format as `delay`; independent of it.]]></doc>
		</binding>

	</wo>
</wodefinitions>
