package ajaxplayground.components.gallery;

import com.webobjects.appserver.WOContext;

import ajaxplayground.components.PlaygroundPage;

/**
 * Gallery: AjaxUpdateContainer. Placeholder - to be built out: a minimal AjaxUpdateContainer with a
 * counter / timestamp and an update link, plus a morph toggle, as the baseline behavioural test.
 */
public class GalleryUpdateContainer extends PlaygroundPage {

	public GalleryUpdateContainer( WOContext context ) {
		super( context );
	}
}
