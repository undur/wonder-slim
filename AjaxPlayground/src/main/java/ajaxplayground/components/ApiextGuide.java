package ajaxplayground.components;

import com.webobjects.appserver.WOContext;

/**
 * A static prose guide to the {@code .apiext} extended element-API format - written for fellow WO devs to
 * read and give feedback on. Pure HTML in the .wo template; no dynamic logic.
 */
public class ApiextGuide extends PlaygroundPage {

	public ApiextGuide( WOContext context ) {
		super( context );
	}
}
